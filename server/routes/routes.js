import express from "express";
// Importar controladores

const router = express.Router();

// router.get("/", ); <-- Obtener página principal
// router.post("/", ) <-- Enviar solicitud

export default router;
